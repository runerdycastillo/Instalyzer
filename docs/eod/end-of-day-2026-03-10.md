# End Of Day - March 10, 2026

## What We Finished

- finalized the product name: `Instalyzer`
- replaced visible `f&f` branding in the current static shell with `Instalyzer`
- wired the new Instalyzer logo into the nav, footer, and Tool 1 shell
- updated the favicon to use the new Instalyzer logo asset
- tuned the nav/footer logo spacing so the mark feels cleaner and less oversized
- aligned on keeping the app nav `icon-only` rather than forcing a wordmark into the header

## What Is Now Stable

- product name is now locked as `Instalyzer`
- the centered logo in the app nav is the preferred direction for the current shell
- the app/workspace header should stay icon-first and minimal
- the current static homepage remains the reference surface before the Next.js migration

## What Still Needs Work

- the homepage hero visual still reflects the older brand system
- the homepage needs a more cohesive Instalyzer visual language
- the homepage still needs a stronger tools/platform story
- Tool 2 is still not formally locked
- the Next.js migration has not started yet

## Priority For Tomorrow

1. replace the old hero visual/background with a new Instalyzer-branded direction
2. add a homepage tools explainer section with clickable tool states and descriptions
3. reorder the homepage so the flow is:
   `hero -> how it works -> tools -> tutorial -> create dataset`
4. sharpen homepage copy around:
   `upload once -> create reusable datasets -> run multiple tools`
5. keep future-tool messaging concrete and defensible
6. start the Next.js migration only after the homepage reference is solid

Tomorrow is Wednesday, March 11, 2026.

## Homepage Direction Notes

- the home page should feel like an Instagram intelligence workspace, not a one-tool utility
- the new visual direction should echo:
  - focus
  - targeting
  - analysis
  - precision
- the old hero art should be replaced with a branded visual system that matches the new logo
- the nav should remain clean and centered around the logo mark, without adding a redundant wordmark

## Tools Section Direction

- add a dedicated homepage section for `available tools`
- use workspace-style tool pills/cards so the marketing surface feels connected to the product UI
- clicking a tool should switch a main description panel rather than opening the tool itself
- each tool state should explain:
  - what it does
  - what dataset/input it uses
  - what output the user gets
  - whether it is live now or coming soon

Recommended tools to feature first:

1. `Not Following Back`
2. `Audience Insights`
3. `Reach Summary`
4. `Content Interaction Summary`

## Positioning Reminder

- keep reinforcing:
  - upload once
  - create reusable datasets
  - run multiple tools
- relationship tools should still be framed around `JSON` and `all time` exports
- future tools should be shown as realistic next modules, not vague promises

## Transition Reminder

- do not begin the Next.js migration from a half-decided homepage
- use the current static build as the reference spec
- once the homepage structure and visual direction feel locked, begin the `web/` migration
